
<%= form_for(@err_status, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.form_group :statusname, class: 'row' do |f| %>
      <%= f.label :statusname, class: 'control-label col-md-2' %>
      <div class='col-12 col-md-10'>
        <%= f.text_field :statusname, class: 'form-control' %>
        <%= f.error_messages %>
      </div>
    <% end %>

    <%= f.form_group :errvalue, class: 'row' do |f| %>
      <%= f.label :errvalue, class: 'control-label col-md-2' %>
      <div class='col-12 col-md-10'>
        <%= f.number_field :errvalue, class: 'form-control' %>
        <%= f.error_messages %>
      </div>
    <% end %>

  </div>

  <div class="d-flex">
    <div class="ml-auto">
      <%= f.submit class: 'btn btn-primary' %>
      <%= link_to "Cancel", err_statuses_path, class: 'btn btn-outline-default' %>
    </div>
  </div>
<% end %>

