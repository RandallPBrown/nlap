
<%= form_for(@err_name, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.form_group :errname, class: 'row' do |f| %>
      <%= f.label :errname, class: 'control-label col-md-2' %>
      <div class='col-12 col-md-10'>
        <%= f.text_field :errname, class: 'form-control' %>
        <%= f.error_messages %>
      </div>
    <% end %>

  </div>

  <div class="d-flex">
    <div class="ml-auto">
      <%= f.submit class: 'btn btn-primary' %>
      <%= link_to "Cancel", err_names_path, class: 'btn btn-outline-default' %>
    </div>
  </div>
<% end %>

